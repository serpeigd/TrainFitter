# Dependencias de TrainFitter
#
# El pipeline por defecto (motor="reglas": rutina, dieta y validador) es 100%
# Python estándar. NO necesita nada de lo de abajo ni ninguna API key.
#
# UI (opcional) — solo si quieres usar el panel del entrenador en ui/app.py:
streamlit>=1.38.0

# Parser de analítica (opcional) — solo si quieres que el PDF de analítica
# adjunto en la ficha se lea de verdad (agents/analytics_parser.py). Librería
# local, sin coste, sin API:
pdfplumber>=0.11.0

# Motor LLM (opcional) — solo si en el futuro se usa motor="llm" (llamadas
# reales a la API de Anthropic en vez del motor de reglas gratuito):
# anthropic>=0.40.0
# python-dotenv>=1.0.0

# Conector Gmail (opcional) — solo si vas a crear borradores reales desde
# mcp/gmail_client.py. Requiere configurar tu propio proyecto en Google
# Cloud Console primero (ver el docstring de ese módulo); gratis, pero no
# funciona "de fábrica" como pdfplumber o streamlit:
# google-api-python-client>=2.150.0
# google-auth-httplib2>=0.2.0
# google-auth-oauthlib>=1.2.0

# Tests (opcional) — solo si quieres ejecutar la suite en tests/. Gratis,
# no llama a ninguna API:
pytest>=8.0.0

# Lint (opcional) — solo si quieres ejecutar `ruff check .` en local (ver
# pyproject.toml). Gratis, wired en CI:
ruff>=0.6.0
